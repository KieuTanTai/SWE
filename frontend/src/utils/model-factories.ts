/**
 * Model Factory Functions
 * Centralized factory functions to create default model objects
 * Use these to avoid repetitive object initialization
 */

// Re-export all factory functions
export { createDefaultAccount } from "../interfaces/account";
export { createDefaultRole } from "../interfaces/role";
export { createDefaultPerson } from "../interfaces/person";

// Add more factory functions as needed for other models
